import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function SavedPlacesPage() {
  return (
    <PlaceholderPage
      title="Saved places"
      description="This route can store bookmarked attractions, restaurants, stays, and local experiences."
    />
  );
}
