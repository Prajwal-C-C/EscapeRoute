import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function LoginPage() {
  return (
    <PlaceholderPage
      title="Sign in"
      description="This page is ready for the authentication form. For now it prevents the imported navigation from landing on a 404."
    />
  );
}
