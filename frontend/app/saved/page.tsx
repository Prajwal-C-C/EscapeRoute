import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function SavedPage() {
  return (
    <PlaceholderPage
      title="Saved places"
      description="This route can hold bookmarked attractions, restaurants, stays, and itinerary ideas."
    />
  );
}
